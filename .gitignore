# dependencies
node_modules/
.pnpm-store/

# cloudflare / wrangler
.wrangler/
worker-configuration.d.ts
.dev.vars
.dev.vars.*
!.dev.vars.example

# env / secrets (never commit)
.env
.env.*
!.env.example

# build / test artifacts
dist/
coverage/
*.tsbuildinfo

# logs / os
*.log
.DS_Store

# D1 backups / exports — plaintext tenant data (LGPD). NEVER commit.
backups/
*.sql.bak
.gstack/
